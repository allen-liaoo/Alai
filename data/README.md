Each data source contains `/raw` and `/processed` directories. `/processed` directory should contain `.txt` files of the form:
```
##[Sent| Recieved]
[Message]

##[Sent| Recieved]
[Message]
```
With new line inbetween each message.